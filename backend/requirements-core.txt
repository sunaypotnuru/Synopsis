# ============================================================
# NETRA AI - CORE API REQUIREMENTS
# ============================================================
# Lightweight dependencies for the main API service
# ============================================================

# ── CORE API DEPENDENCIES ──────────────────────────────────
--extra-index-url https://download.pytorch.org/whl/cpu

fastapi==0.115.0
uvicorn[standard]==0.30.1
websockets>=13.0
python-dotenv==1.0.0
httpx>=0.27.0
requests>=2.31.0
python-multipart==0.0.9
pydantic-settings>=2.0.0
pydantic[email]>=2.0.0
PyJWT>=2.0.0
email-validator>=2.0.0

# ── DATABASE & AUTHENTICATION ──────────────────────────────
supabase>=2.10.0

# ── VIDEO CONSULTATIONS ────────────────────────────────────
livekit-api==0.5.1

# ── BASIC IMAGE PROCESSING ─────────────────────────────────
Pillow>=10.0.0
numpy>=1.24.0

# ── AI INTEGRATION ─────────────────────────────────────────
google-generativeai>=0.8.0

# ── SYSTEM MONITORING & UTILITIES ──────────────────────────
psutil>=5.9.0
apscheduler>=3.10.4

# ── PAYMENT INTEGRATION ────────────────────────────────────
razorpay>=1.4.0

# ── SMS & NOTIFICATIONS ─────────────────────────────────────
twilio>=9.0.0
sendgrid>=6.11.0

# ── TEMPLATE ENGINE ─────────────────────────────────────────
jinja2>=3.1.2

# ── SECURITY & RATE LIMITING ─────────────────────────────────────
slowapi>=0.1.9
python-magic-bin>=0.4.14; sys_platform == "win32"
python-magic>=0.4.27; sys_platform != "win32"
sentry-sdk
faster-whisper>=1.0.0
sentence-transformers

# ── SELF-HOSTED AI (DeepSeek-R1 via Ollama - FREE, HIPAA-Compliant) ──────────
ollama==0.4.4


# Phone number validation
phonenumbers>=8.13.0
